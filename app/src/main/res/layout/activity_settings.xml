<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context=".activities.SettingsActivity">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/settingsHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/setting_header"
            android:text="@string/settings"
            android:layout_centerHorizontal="true"/>

        <TextView
            android:id="@+id/vibrationText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/options_text"
            android:text="@string/vibration"
            android:layout_below="@+id/settingsHeader"/>

        <ToggleButton
            android:id="@+id/vibrationToggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="New ToggleButton"
            android:layout_gravity="center_horizontal|top"
            android:layout_alignTop="@+id/vibrationText"
            android:layout_toRightOf="@+id/rHandedText"
            android:layout_toEndOf="@+id/rHandedText" />

        <TextView
            android:id="@+id/rHandedText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/options_text"
            android:text="@string/rHanded"
            android:layout_below="@id/vibrationText"/>

        <ToggleButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="New ToggleButton"
            android:id="@+id/rHandedToggle"
            android:layout_gravity="center_horizontal|top"
            android:layout_below="@id/vibrationText"
            android:layout_toRightOf="@+id/rHandedText"/>

    </RelativeLayout>

</FrameLayout>
